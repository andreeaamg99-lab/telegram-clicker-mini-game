import React, { useState } from 'react'; import { Clicker } from"./components/Clicker/Clicker"; import "./styles/global.scss"; import styles from"./App.module.scss"; import { ResourceCounter } from "./components/ResourceCounter"; import { MainObject } from "./components/MainObject"; import { Payment } from "./components/Payment";import { ProgressBar } from "./components/ProgressBar"; import { Upgrades } from"./components/Upgrades"; export const App = () => { const [coins, setCoins] = useState(0);const [crystals, setCrystals] = useState(0); const [progress, setProgress] = useState(0);return ( <div className={styles.container}> <ResourceCounter coins={coins} crystals={crystals} /> <ProgressBar progress={progress} /> <MainObject /> <Clicker setCoins={setCoins} setProgress={setProgress} /> <Upgrades coins={coins} setCoins={setCoins} /> <Payment /> </div> ); };
